# 迅雷云盘 Cookie/存储分析报告

> 日期：2026-06-22  
> 工具：curl / whatweb / Kali Linux

## 一、HTTP 架构

| 端点 | 状态 | 服务器 | 说明 |
|:-----|:----:|:-------|:-----|
| `pan.xunlei.com` | 200 | 无（未暴露） | 直接200，无重定向 |

## 二、安全头部

| 头部 | 值 |
|:-----|:----|
| `X-Frame-Options` | 缺失 ❌ |
| `X-XSS-Protection` | 缺失 ❌ |
| `Strict-Transport-Security` | 缺失 ❌ |
| `X-Content-Type-Options` | 缺失 ❌ |
| `Content-Security-Policy` | 缺失 ❌ |

> ⚠️ **安全配置最弱：** 所有常见安全头部均缺失，共计 0 个安全头部。在所有分析的网盘中安全配置最弱。

## 三、认证机制

### 3.1 Cookie 体系

| Cookie | 域 | HttpOnly | 用途 |
|:-------|:---|:--------:|:-----|
| 无 | — | — | 未登录态下无Cookie下发 |

> 登录后预计会有迅雷自有体系 Cookie（需登录确认）。

### 3.2 localStorage（预计，需登录确认）

> 需登录后才能确认 localStorage Key 名。预计为迅雷自有体系命名风格。

### 3.3 技术栈

| 组件 | 说明 |
|:-----|:------|
| 前端 | HTML5 |
| 代理 | 经反向代理转发（`via: a012523` Header） |
| 安全配置 | 无 |
| SSO | 迅雷自有统一认证体系 |

> `via: a012523` 表明请求经过了某个代理/负载均衡器节点。

### 3.4 认证流程（推测）

```
访问 pan.xunlei.com
  ↓
检查登录态（Cookie / Token）
  ↓
未登录 → 迅雷统一登录页面
       ↓
登录后设置认证 Cookie（域：.xunlei.com）
```

## 四、关联 Cookie 域

- `.xunlei.com` — 迅雷主域

## 五、Preset 配置建议

```json
{
  "domain": "pan.xunlei.com",
  "name": "XunleiDrive",
  "nameZh": "迅雷云盘",
  "localStorageKeys": [],
  "knownCookiePrefixes": [],
  "cookieDomains": [".xunlei.com"],
  "notes": "无安全头部配置。需登录后才能确认认证体系。"
}
```

## 六、结论

迅雷云盘在本次分析的网盘中安全配置最弱：所有常见安全头部（X-Frame-Options、HSTS、X-Content-Type-Options、X-XSS-Protection、CSP）均缺失。服务器未暴露 Server 头部。请求经过 `a012523` 代理节点。采用 HTML5 前端 + 迅雷自有 SSO 体系。未登录态无 Cookie 下发。需登录确认认证体系详情。
