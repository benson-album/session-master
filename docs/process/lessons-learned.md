# 经验教训与反思

> **更新日期**：持续更新
> **用途**：记录项目开发过程中发现的问题、原因和改正措施，防止同类问题重复发生
> **常态化机制**：每次文档/架构变更后执行「文档健康检查」

---

## 近期问题复盘（2026-06 项目文档体系建立阶段）

### 问题一：独立文档之间内容不一致

**现象**：
- `development-plan.md` 和 `agent-roles.md` 对 PM 职责描述不一致（一个缺"版本发布"，一个缺"日常决策"）
- 签字链：开发计划仍用旧式 `□` 复选框，而独立文档已更新为 `QA→PD→PM→ME`

**根因**：
1. 文档从开发计划独立出去后，两边各自维护，没有做同步机制
2. 签字链修改时只改了通信拓扑图，忘了更新里程碑签名区

**改正措施**：
- ✅ 建立「文档健康检查清单」（见下文）
- ✅ 每次架构变更后，用自动化脚本扫描所有文档的一致性

---

### 问题二：新文档被孤立（无人引用）

**现象**：
- `agent-roles.md`、`agent-document-matrix.md`、`methodology.md` 三份新文档未被其他 5 份主文档引用
- 从 `PRD.md` 无法跳转到角色定义文档

**根因**：
1. 新增文档后没有检查「谁链接了我」
2. 缺少交叉引用审计机制

**改正措施**：
- ✅ 建立「交叉引用矩阵」自动扫描
- ✅ 新增文档后必须更新所有关联文档的 HEADER 引用行

---

### 问题三：方法论文档残留项目特定内容

**现象**：
- `methodology.md` 的沉淀来源写的是"SessionMaster v2.0 项目开发实践"
- 方法论本应是通用的，却绑定了具体项目名

**根因**：
1. 从项目文档提取方法论时，没有做脱敏检查
2. 没有「通用性检查」环节

**改正措施**：
- ✅ 方法论沉淀完成后必须执行「通用性扫描」——grep 项目特定术语
- ✅ 沉淀来源用"多项目开发实践总结"代替具体项目名

---

### 问题四：标题/数量词与实际情况脱节

**现象**：
- 开发计划标题写"4 代理并行协作"但实际是 5 角色（ME+PM+PD+DE+QA）

**根因**：
1. 架构调整后只改了正文内容，忘了同步 HEADER 元信息
2. HEADER 行容易被忽略——它不在正文滚动视野中

**改正措施**：
- ✅ 架构变更后必须检查所有文档的 HEADER 行
- ✅ 用「版本号+角色数量+层级结构」三元组做一致性检查

---

## 文档健康检查清单（常态化）

**触发条件**：以下任一情况发生，必须在提交前执行完整检查
- 新增文档
- 修改角色定义或签字权
- 调整组织架构
- 修改通信模式或流程
- 发布新版本前

### 7 项自动检查

| # | 检查项 | 命令/脚本 | 检查什么 |
|:-:|:-------|:----------|:---------|
| 1 | **Agent 输入文档完整性** | 遍历 Agent 定义表，检查每个输入路径存在 | 路径拼写错误、遗漏 |
| 2 | **文档间交叉引用** | grep 每份文档是否引用了其他 7 份 | 孤文档 |
| 3 | **签字链一致性** | 对比 4 个里程碑签名段的角色顺序 | QA→PD→PM→ME 是否统一 |
| 4 | **HEADER 元信息同步** | 版本号、角色数、层级数三值一致 | 标题与正文不脱节 |
| 5 | **术语统一性** | 模式代号（A~G）、角色名（ME/PM/PD/DE/QA）全文档一致 | 术语漂移 |
| 6 | **通用性检查** | grep 项目特定名（SessionMaster、v2.0 等）仅出现在应出现的位置 | 方法论文档脱敏 |
| 7 | **MD 内部链接可达性** | 提取 `[text](path)` 并检查目标文件存在 | 死链接 |

### 3 项手动检查

| # | 检查项 | 怎么做 |
|:-:|:-------|:-------|
| 8 | **内容逻辑一致性** | 随机抽 2 份文档，对比同一概念（如 PM 职责）的描述是否一致 |
| 9 | **数据流图与实际一致** | 查看文档中的数据流图，对照实际文件路径验证 |
| 10 | **旧格式残留** | 全文搜索是否还有旧版术语（如"房间"替代"配对"、旧式复选框 `□`）|

### 执行方式

```bash
# 一键执行 7 项自动检查
cd /opt/projects/session-master && python3 scripts/doc-health-check.py
```

脚本位置：`scripts/doc-health-check.py`（在每次文档变更前运行）

---

## 反思：为什么会积累这么多问题

| 阶段 | 做了什么 | 没做什么 |
|:-----|:---------|:---------|
| 最初 | 所有内容写在 `development-plan.md` 里 | — |
| 第一次分拆 | 创建了通信目录和卡片模板 | ❌ 没建立独立文档的引用 |
| 第二次分拆 | 创建了 `agent-roles.md` 等 3 份独立文档 | ❌ 没做交叉引用审计 |
| 第三次分拆 | 创建了 `methodology.md` | ❌ 没做通用性脱敏 |
| 架构调整 | 改了通信拓扑图和角色定义 | ❌ 没同步更新所有里程碑签名区 |
| 每次提交 | 只关注新增的内容 | ❌ 没全局扫描已存在的文档 |

**核心教训**：新增或修改文档时，不能只看「我改了啥」，必须看「其他文档是否因此需要同步更新」。

**常态化机制**：把文档健康检查清单做成 `scripts/doc-health-check.py` 脚本，每次文档变更前运行。脚本检查不通过，不提交。

---

## 版本管理策略

> **背景**：v1.0（master）和 v2.0（develop）两个版本并行维护。master 沿用传统开发方式，develop 采用新设计的多 Agent 规范流程。

### 分支策略

| 分支 | 版本 | 开发方式 | 用途 |
|:-----|:----:|:---------|:-----|
| `master` | v1.x | 传统开发（简单直接） | 稳定发布版，日常维护、bug 修复 |
| `develop` | v2.0 | 多 Agent 规范流程（含 PM/PD/DE/QA 角色） | 新版本架构重构 |

### 核心原则

1. **各自独立**：master 和 develop 分别维护，不互相合入
   - bug 修复如果适用于两个版本 → 各自手动移植（非 merge）
   - 如果修复影响架构兼容性 → 各版本独立决定是否采用
   - 过程文档（`docs/process/`）变更需在两个分支都执行一遍

2. **开发方式各异**：
   - **master（传统）**：直接在 master 上修改、提交、发布，无需角色分工
   - **develop（规范）**：使用 PM 派卡 → PD 设计 → DE 开发 → QA 验证 → PM 终签的完整流程

3. **文档隔离**：
   - `docs/process/` → 版本无关，两个分支都保留
   - `docs/v2.0/` → 仅 develop 分支
   - master 的架构信息通过代码注释和 README 维护

### 版本号规则

| 分支 | 格式 | 示例 |
|:-----|:-----|:-----|
| `master` | `v1.x.y`（语义版本） | `1.5.14` → `1.6.0` → `1.7.0` |
| `develop` | `v2.0.0-dev`（开发中） | `2.0.0-dev` → 发布时改为 `2.0.0` |
| 发布后 | develop 合并替换 master | master 替为 `2.0.0`，旧 master 存档为 `v1.x` 分支 |

### 版本升级检测

> **设计原则**：扩展内置自动检测新版本，用户无需手动检查。

**检测机制**：

1. **首次启动**：onInstalled 后 10 秒执行首次检测
2. **定时检测**：每 6 小时通过 `chrome.alarms` 自动检查
3. **用户手动**：popup 可调用 `checkUpdate` 消息强制检测
4. **缓存策略**：1 小时内不重复请求（节省流量）

**版本比较**：

- 从 GitHub 读取 `VERSION` 文件（`master` 分支 = 稳定版）
- 语义版本比较（`compareVersions` 函数）：`1.5.14` → `1.6.0` ≥ `1.5.15`
- `-dev` / `-beta` 后缀视为开发版，不触发升级通知

**通知方式**：

| 场景 | 行为 |
|:-----|:-----|
| 发现新版本 | 浏览器角标显示 `NEW` + 系统通知 |
| `develop` 测试版 | 可通过设置面板切换更新源到 `BETA_URL` |
| 更新源切换 | 用户设置 `update_url` 指向 develop 分支 VERSION |
| 升级后 | 角标清除，下次检测到更高版本再提示 |

### 本次遗漏复盘

> **经验**：升级检测功能添加到代码后，漏了更新帮助文档和重建打包文件。

| 漏了什么 | 怎么发现的 | 以后怎么做 |
|:---------|:-----------|:----------|
| 帮助页 FAQ 没写「NEW」角标说明 | 用户提醒排查文档 | 功能变更后检查帮助页是否需要同步更新 |
| zip 包没重建（含旧代码） | 用户问「项目有同步吗，包括打包文件」 | 每次功能变更后执行 `bash scripts/build.sh` |
| 帮助页静态版本号没更新 | 排查文档时顺带发现 | ✅ **已修复：改为从 manifest.json 动态读取** |

**核心教训**：凡是有"改了 X 要记得改 Y"的地方，都说明 Y 应该引用 X 而不是硬编码。版本号只是一个例子——端口号、应用名、URL 等静态值都应该遵循单一数据源原则。

**单一数据源原则**：
- `manifest.json` = 版本号的唯一来源
- `config.js` = 运行时动态读取 manifest.json 的 version
- `help.html` = JS 动态填充，不留静态版本号
- `build.sh` = 构建时验证 VERSION 文件 == manifest.json 版本
- 其他静态值（端口 5789、应用名等）也应逐步改为引用方式

**常态化机制**：每次提交前自检清单（追加到原有 10 项检查后）：

| # | 检查项 | 怎么做 |
|:-:|:-------|:-------|
| 11 | **帮助页同步** | 检查帮助页是否需要更新（FAQ/说明/版本号） |
| 12 | **zip 包重建** | `bash scripts/build.sh` 重建并清理旧包 |
| 13 | **经验教训记录** | 本次是否发现了可以沉淀的经验？ |
| 14 | **弹窗滚动检查** | 内容超长时 footer 是否可见、有无滚动条、overflow-y:auto 是否生效 |

---

## 近期问题复盘（2026-06-22 v2.0 审计阶段）

### 问题五：存储键共用导致配置互相覆盖

**现象**：
- `config.js` 中 `STORAGE_KEYS.SYNC_CONFIG` 和 `STORAGE_KEYS.SERVER_SYNC_CONFIG` 都是 `'cloud_sync_config'`
- P2P 配置（signalUrl、p2pDeviceName）和服务器配置（serverUrl、pairKey、deviceName）存储在同一个 key 下
- 如果用户同时使用了两种同步模式，一方配置可能被另一方覆盖

**根因**：
1. copy-paste 定义 key 时没有在最后一步检查值是否唯一
2. 两个 key 在各自模块（P2P 和服务器同步）中独立使用，运行时没有交叉冲突的表征——只有同时使用两种模式时才会暴露

**改正措施**：
- ✅ 立即修复：`SERVER_SYNC_CONFIG` → `'cloud_server_sync_config'`
- ✅ 新增 STORAGE_KEYS 定义检查清单：每次新键必须确保与所有现有键都不重复
- ✅ master 分支同步修复

### 问题六：工时估算脱离实际代码量

**现象**：
- PRD §7：「约 200 行 popup.js 重写」——实际 2,098 行，偏差 10 倍
- T6「三 Tab 内容重构」估算 2.5h——实际至少 7h
- T8「消息路由」估算 1h——实际 2.5h
- T9「渲染函数化」估算 1h——实际 3.5h

**根因**：
1. 写文档时凭印象估算，没有用 `wc -l` 实际测量
2. 低估了 popup.js 中 innerHTML 拼接 + 事件绑定 + 状态管理的复杂度

**改正措施**：
- ✅ 所有工时估算必须基于 `wc -l` 或 `pygount` 实际读数
- ✅ 开发计划中每个任务的工时备注引用实际行数
- ✅ 全局工时从 12-15h 上调至 24-30h

### 问题七：代码已同步但规划文档未更新

**现象**：
- develop 分支已 cherry-pick 了 master 上全部 8 项 UX 改进（更新横幅、设备状态指示器等）
- 但 v2.0 的 PRD、development-plan、test-plan 均未体现这些功能

**根因**：
1. 功能是先有 master 开发再有 cherry-pick 到 develop，文档没跟上代码
2. 对"分支同步"的理解停留在代码层面，忽略了配套文档也要同步

**改正措施**：
- ✅ 更新 PRD.md §2.5 新增 UX 交互描述
- ✅ 在 PRD 中明确标注"以下功能已在 v1.5.x 实现，v2.0 继承"
- ✅ 后续规范：功能 cherry-pick 后必须检查三份规划文档是否需要更新

### 问题八：缺少文档归口管理

**现象**：
- 审计发现 7 份核心文档没有明确的维护责任人
- PRD 更新没有通知 dev-plan 和 test-plan 的流程
- 变更时依赖人工自觉性，没有约束机制

**根因**：
1. 项目初期文档少，靠记忆管理可行
2. 文档体系扩展后（12 份文档），没有建立治理规则

**改正措施**：
- ✅ 在 `agent-document-matrix.md` 新增「文档归口管理规范」章节（7 份文档的维护责任人 / 审核人 / 更新触发条件 / 生命周期）
- ✅ 定义文档更新流程（起草→审核→发布→通知）
- ✅ 定义跨文档一致性原则（PRD 是源头、dev-plan 必须覆盖 PRD、test-plan 必须覆盖 PRD+dev-plan）
- ✅ 定义版本号对齐规则（draft→beta→stable 各阶段要求）

---

### #19️⃣ ZIP 解压目录名未包裹版本号（反复 Bug）

**时间**：2026-06-22
**现象**：`scripts/build.sh` 输出的 ZIP 解压后文件平铺在根目录，没有 `session-master-vX.X.X/` 父目录。用户反馈后修复，但修复只改了外层 zip 文件名（`OUTPUT_ZIP` 变量），**没改内部目录结构**。

**根因**：
1. 当初只关注了 zip 文件名格式，没验证解压后的目录结构
2. `build.sh` 直接 `cd src && zip -r output.zip .` 天然平铺打包，缺少目录包裹层
3. 修复后没有执行完整的端到端验证（构建→解压→检查目录布局）

**改正措施**：
- ✅ build.sh 改用临时目录 + 版本号子目录方式打包：`mkdir -p _tmp/session-master-vX.X.X/ && cp -r src/* _tmp/session-master-vX.X.X/ && zip -r output.zip session-master-vX.X.X/`
- ✅ 新增自检条款：每次发版前必须执行 `unzip -l *.zip | head -5` 检查第一条目录名是否含版本号

---

### #20️⃣ 发版流程反复不一致（格式/标题/重复发布）

**时间**：2026-06-23
**现象**：v1.5.17 发布过程中出现系列问题：
1. GitHub Release 标题格式反复修改（纯版本号 → 带描述 → 纯版本号）
2. Release body `# v1.5.17` 标题与 GitHub 顶栏版本号重复
3. 草稿 Release 残留未清理（双 v1.5.16 问题）
4. Release body 格式跟用户期望的不一致（平铺列表 vs 分类分组 vs 分隔线）
5. changelog.json 结构反复重构（数组 → 分类对象 → 加 title → 去 title）

**根因**：
1. 没有「先确认再执行」的流程 — 对 Release 格式的改动全凭猜测后立刻执行，用户看了不满意又改回来
2. 手动拼 curl 参数发布，缺少完整发版脚本 — 每步容易遗漏或拼错
3. changelog 结构变更时没同步更新 help.js 渲染函数，导致多轮适配

**改正措施**：
- ✅ 创建 `scripts/release.sh` 一键发版脚本（构建→创建 Release→上传附件，一次完成）
- ✅ 确定 Release 命名规范：`name = 'vX.X.X'`，body 不重复版本号
- ✅ 确定 Release body 结构：`分类名称 + ----分隔线 + 条目列表`
- ✅ changelog.json 统一为分类分组结构（字典 key = 分类名）
- ✅ help.js 渲染同时支持新版（分类对象）和旧版（数组）两种 changelog 格式
- 📌 **待完成**：发布前应在用户面前预览 Release body，确认后再执行（release.sh 已有预览功能）

---

### #2️⃣1️⃣ 弹窗底部内容被裁切（footer 不可见）

**时间**：2026-06-27

**现象**：弹窗底部的「📖 使用说明 / 📋 导出日志 / 本插件仅供学习研究使用」在 popup 中不可见，被弹窗视口底部裁切，无滚动条可拖动到底部。

**根因**：
1. `popup.css` 中 `body` 没有设置 `overflow-y` 属性（默认 `visible`）
2. Chrome 扩展弹窗使用 `default_popup` 时，弹窗窗口有最大高度限制（约 600px），内容超出的部分直接裁切，不会出现滚动条
3. 之前只关注了去掉 `min-height: 400px` 让弹窗自适应高度，但忽略了「超长内容如何滚动」的 scroll 场景

**改正措施**：
- ✅ `body` 添加 `overflow-y: auto; overflow-x: hidden;` — 内容超出弹窗时自动出现滚动条
- ✅ `html` 也添加相同规则（Chrome 扩展需要双重保障）
- ✅ 自检清单 #7（UI视觉一致性）验证范围拓展：不能只看「不溢出」，还要检查「溢出后能否滚动」
- ✅ 恢复执行构建脚本重新打包 ZIP

---

### #2️⃣2️⃣ 同一问题反复改方案、跳过自检、发版漏步骤（表现不稳定）

**时间**：2026-06-22

**现象**（暴露了 4 个关联问题）：

1. **footer 问题改了 3 轮才定**：先加 `overflow-y: auto` → 换成 flex 粘性布局（用户否决）→ 再改成 JS 测量高度。每轮都是完整提交推送，浪费大量回合。
2. **提交前不自检**：用户直接质问"你提交前是不是没有遵循自查原则"。
3. **忘了发版**：代码改了、推送了，但没创建 Release，用户问"有推送更新吗"才发现。
4. **changelog 日期设到了未来**：v1.5.17~1.5.22 共 6 个版本的日期填成了 2026-06-23~27。

**根因**：

1. **方案确认前就动手** — footer 问题没有先问用户期望的交互（固定底部 vs 内容高度），直接选了 flex 方案，做完了被否掉重来。
2. **同一轮改动太多目标混在一起** — footer 修复 + 清除确认弹窗 + UI 清爽化 + 消重提示 + ... 多个改动交叉提交，前后不一致。
3. **发版不是常态化动作** — 代码改了≠用户能用上，少了一道「发版检查」的自动触发器。
4. **日期填写不用心** — changelog 日期随手写了个未来的数字，没有核实今天几号。

**改正措施**：

- ✅ **先确认方案再编码**：涉及布局/交互变更时，先给用户 2~3 个可选方案并说明差异，确认后再动手。参考 `writing-plans` skill 的流程。
- ✅ **改代码前先跑自检清单**：每次提交前必须至少过一遍 #7 UI视觉一致性、#12 ZIP包重建、#13 经验教训记录。
- ✅ **代码变更后主动问「要不要发版」**：代码推送后如果尚未创建新 Release，自动问用户是否需要发版。
- ✅ **写日期时核实当前日期**：`date +%Y-%m-%d` 确认今天日期，不靠记忆/推测。
|- ❌ **（教训）不要在同一个 session 里跨多个不相关的功能反复横跳** — 单次工作聚焦一个目标，完成并发版后再进入下一个。

---

## 完整复盘：2026-06-22 Session 回顾

### 本次会话时间线（暴露出问题的节点）

| 顺序 | 用户要求 | 我做了什么 | 问题 |
|:----:|:---------|:-----------|:-----|
| 1 | 排查弹窗 footer 不可见 | 加 `overflow-y: auto` | 只加滚动条，footer 仍需滚动才见 |
| 2 | 要 footer 不滚就可见 | 直接改 flex 粘性布局 | ❌ **没问方案就动手**，用户否决 |
| 3 | "这个方案不好" | 换成 JS 测量高度 | 终于对了，但浪费 2 轮 |
| 4 | 验证清除 Cookie 确认弹窗 | 做了自定义 Modal | ✅ 用户接受 |
| 5 | "有推送更新吗" | 只推了代码，没发 Release | ❌ **忘了发版** |
| 6 | 首页按钮太重 | 降低按钮层级 | ✅ 用户接受 |
| 7 | "怎么更新日志是未来时间" | 6 个版本日期填了未来 | ❌ **没核实日期** |
| 8 | "有反思总结吗" | 写了 #22 | ❌ 但复盘不完整，需要再补 |
| 9 | 保活 URL 防重 | 修复了 `addHeartbeat` | ✅ 但用户说"顺便排查"— |
| 10 | "顺便排查是否存在类似问题" | 查了其他 add* 操作 | ⚠️ 应该**主动**查，而不是等用户说 |

### 暴露的 5 个系统性问题

#### 问题 1：急于动手，先做再想

footer 布局改了 3 轮，如果一开始就问用户「你期望弹窗高度怎么定？A. 固定底部 B. 自适应内容 C. 其他」，5 分钟就能确认方向，不用走 flex → 被否 → 重做的弯路。

**新规矩**：涉及布局/交互变更，先给选项再编码。

#### 问题 2：自检清单不是自动的

每次提交前「应该」跑自检，但没有真正的约束机制。这次被用户抓到 3 次（不自检、不发版、日期错）。

**新规矩**：本 skill（写作中）增加「提交前防火墙」模式。

#### 问题 3：同一会话多个目标交叉

这一轮在改 footer 的同时，还做了清除确认弹窗、UI 清爽化、防重检查、changelog 修复……多个改动杂在一起，哪个都没做到位。

**新规矩**：单次会话聚焦一个目标。完成了？问用户「要不要发版？」——发完再进下一个。

#### 问题 4：边做边漏

- 代码改了但没发 Release → 用户以为没进度
- 加保活时只写了添加逻辑，没想「加了还能不能再加一次」 → 用户指出防重缺失
- 写 changelog 日期没查日历 → 写到了未来

**新规矩**：每个 `add*` / `create*` 函数写完，都要在文件中做「防重检查」，使用 `duplicate-check-pattern` skill。

#### 问题 5：没有主动查漏的习惯

用户说「顺便排查是否存在类似问题」才去查其他的 add* 操作——应该在修复 `addHeartbeat` 时就主动全查一遍。

**新规矩**：发现某类 Bug 时，**顺藤摸瓜查同类**，不等用户说。

### 本次沉淀

| 沉淀物 | 说明 |
|:-------|:------|
| `duplicate-check-pattern` skill | 防重检查标准模式 + 覆盖表 + 检查清单 |
| lessons-learned.md #22 | 完整复盘 |
| 长期记忆更新 | 方案确认 + 主动问发版 + 日期核实 + 自检 |

### #2️⃣3️⃣ 站点存储分析方法论抽象及时，但登录态数据获取受限

**时间**：2026-06-23
**场景**：分析国内主流网盘（百度/阿里/夸克/微云/天翼/迅雷/123/奶牛）的 Cookie 和 localStorage 存储模式

**做得好的**：
1. **方法论及时沉淀** — 分析完 4 个视频站后立即抽象为 `website-storage-analysis` skill，后续网盘分析直接复用 6 阶段流程，效率显著提升
2. **工具链复用** — Kali SSH + curl/whatweb 侦察命令标准化，每个站点探测参数一致，结果可比
3. **跨体系模式识别** — 中国互联网"三大 SSO 体系"（百度 BAIDUID、阿里 Mtop、腾讯 TOK）识别准确，cookieDomains 覆盖了 SSO 兄弟域
4. **诚实标注未知项** — 无法获取登录态 localStorage 的 Key 都标注了 `（需登录确认）`，不推断冒充已知数据

**不足的地方**：
5. **浏览器环境缺失** — Chrome 未安装，无法通过 DevTools 采集登录态后的实际 localStorage 数据，报告中有 5 个站点的 Key 是"需登录确认"状态
6. **外部 CDN 访问受限** — Cloudflare 等 CDN 拒绝数据中心的服务器 IP（APKPure、APKMirror 等），导致后续下载 GitHub Android APK 的任务失败

**改正措施**：
- ✅ 分析前置任务：在进行存储分析之前，先确认能否安装浏览器或访问站点 HTML 页面
- ✅ 对于"需登录确认"的 Key，在预设文件中设空数组并备注，不做无依据的推测
- ✅ APK/文件下载：如果外部 CDN 不可达，应向用户提供 Google Play 官方链接作为替代方案


---

### 问题N：站点分析预设存在猜造成分，未标注置信度

**现象**：
- 阿里云盘、夸克网盘等站点的 localStorageKeys 为推测值，未标注「需登录确认」
- 天翼云盘 X-Frame-Options 拼写错误记录有误（缺少字母I）

**根因**：
1. 分析时无法获取登录态（无Chrome，无用户登录session）
2. 直接凭SSO体系推断Key名，未加置信度标注

**改正措施**：
- ✅ 所有低置信度字段标注「需登录态确认」
- ✅ 预设结构增加 authType、hasDeviceFingerprint 等元数据字段
- ✅ 分析报告中明确列出已知/推测/未知

---

### 问题N+1：跨站点类型分析未抽象通用方法论

**现象**：
- 视频站点分析了一套方法，网盘站点又新分析一次
- 两个分析流程高度重复，但未抽取通用层

**根因**：
1. 第一次分析（视频）没考虑扩展到其他类型
2. 没有预先设计通用分析方法论

**改正措施**：
- ✅ 创建 website-storage-analysis 技能，覆盖完整6阶段+常见陷阱
- ✅ 加入跨类型扩展章节（视频→网盘→电商→社交→金融等）
- ✅ 分析方法论本身应独立于任何具体项目

### 接下来要保持的

- ✅ 改代码前先问方案 → 否则不动手
- ✅ 写完 add* 自动加防重检测
- ✅ 改完代码先问「发版吗」→ 再进入下一个任务
- ✅ 写日期前 `date +%Y-%m-%d` 确认

### 常态化复盘机制

> **问题：** 只有当用户问到才复盘，缺少主动反思的节奏。
>
> **改正：** 每次完成一个工作周期（发版 / 分析产出 / 重大修复）后自动执行以下 3 步：

1. **亮点**：这轮做对了什么（方法论抽象、诚实标注、防重检测等）
2. **改进**：暴露了什么系统性问题（浏览器缺失、CDN 封锁、先做再想等）
3. **沉淀**：更新了什么（技能 / 记忆 / 文档 / 检查清单）

复盘时机：发版后、分析产出后、用户明确问"有反思吗"时。
